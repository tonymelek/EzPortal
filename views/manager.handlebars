<div class="jumbotron">
    <h1 class="text-center text-success">Welcome {{name}}</h1>
</div>
<div class="row justify-content-center">
    <div class="card col-lg-5">
        <div class="card-header">Tasks to sign off</div>
        <ul>
            {{#each tasks}}
            {{#unless completed}}
            {{> tasks/tasks-block completed=true}}
            {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="card col-lg-3">
        <div class="card-header">Employee List</div>
        {{List of employees populated and clickable to then bring up their information and tasks}}
    </div>
</div>