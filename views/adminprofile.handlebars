<nav class="navbar navbar-expand-md bg-dark navbar-dark">
    <div class="container">
        <a class="navbar-brand" href="#">
            <h2> Admin | {{admin.first_name}}</h2>
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/admin-prof/{{token.token}} active">My Profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin-dept/{{token.token}}">Departments</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="/admin-role/{{token.token}}">Roles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin-user/{{token.token}}">Employees</a>
                </li>
            </ul>

        </div>
    </div>
</nav>
<div class="container my-5">
    <div class="row">
        <div class="col-md-5 col-12">
            <div class="card mb-5">
                <div class="card-header">
                    <h2 class="text-center"> My Tasks</h2>
                </div>
                <div class="card-body">
                    <div id='tasks-list'>
                        {{#each tasks}}
                        {{#unless dataValues.approved}}
                        {{#if dataValues.completed}}
                        <div class="d-flex justify-content-between my-2 p-2 itemA w-95 mx-auto ">
                            <div class="font-weight-bold" id='task-{{dataValues.id}}'>
                                {{dataValues.PreDef.dataValues.title}} ||
                                {{this.dataValues.assigned_by.dataValues.first_name}}
                                {{this.dataValues.assigned_by.dataValues.last_name}}
                            </div>

                        </div>
                        {{else}}
                        <div class="d-flex justify-content-between my-2 p-2 item w-95 mx-auto ">
                            <div class="font-weight-bold" id='task-{{dataValues.id}}'>
                                {{dataValues.PreDef.dataValues.title}} ||
                                {{this.dataValues.assigned_by.dataValues.first_name}}
                                {{this.dataValues.assigned_by.dataValues.last_name}}
                            </div>
                            <div>
                                <a href="#" class="d-inline-block px-2 completed text-success"
                                    data-assignedto='{{dataValues.assignedto}}' id="complete-{{dataValues.id}}"><i
                                        class="fas fa-check"></i></a>
                            </div>
                        </div>
                        {{/if}}

                        {{/unless}}
                        {{/each}}
                    </div>

                </div>
            </div>
        </div>
        <div class="col-md-7 col-12">
            <div class="card mb-5">
                <div class="card-header">
                    <h3 class="text-center">Change password</h3>
                </div>
                <div class="card-body">
                    <form action="" method="post" id="change-pass">
                        <div class="form-group">
                            <label for="old">Old Password</label> <br>
                            <input class="form-control" type="password" name="old" id="old">
                        </div>
                        <div class="form-group">
                            <label for="new1">New Password</label> <br>
                            <input class="form-control" type="password" name="new1" id="new1">
                        </div>
                        <div class="form-group">
                            <label for="new2">Confirm New Password</label> <br>
                            <input class="form-control" type="password" name="new2" id="new2">
                        </div>
                        <button class="btn btn-primary" type="submit">Change Password</button>
                    </form>
                </div>
            </div>


        </div>
    </div>
</div>

<script>
    localStorage.setItem("ezPortal", '{{token.token}}')

    $(document).on("click", ".completed", function (e) {
        e.preventDefault();
        const toComplete = this.id.split('-')[1]
        console.log(toComplete)
        $.ajax({
            type: 'put',
            url: `../api/complete-task`,
            headers: {
                'Authorization': `bearer ${localStorage.getItem('ezPortal')}`
            },
            data: {
                task_id: toComplete

            }
        }).then((result) => {
            console.log(result)
            location.reload()
        })
    })


</script>
{{>change-pass}}